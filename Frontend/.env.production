# Production API Configuration
# Backend merged into Indus360 (one host). BulkImport API now lives under /bulk/api on api-indorb.
# Old (pre-merge, standalone exceljet host): https://exceljet.indusanalytics.co.in/api
VITE_API_BASE_URL=https://api-indorb.indusanalytics.co.in/bulk/api

# Environment
# Vite handles NODE_ENV automatically based on build commands.
# Do not set it here to avoid warnings.
